echo "🌀 Running post-merge tasks…"

# Frontend
cd frontend || exit 1
pnpm run generate:api
pnpm run lint
ruff check . --fix

# Backend
cd ../backend || exit 1
pnpm run generate:api
pnpm run lint
ruff check . --fix

echo "✅ post-merge hooks completed."